# PHP dependencies (restored with `composer install`)
/server/vendor/

# Logs & runtime junk
*.log
console_error.log

# Local-only agent context — never tracked or published
CLAUDE.md

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.idea/
.vscode/

# Note: your personal userscripts in /scripts are ignored by scripts/.gitignore
# composer.lock IS committed (reproducible installs) — see server/.gitignore
